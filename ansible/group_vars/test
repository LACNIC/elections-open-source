-- include_vars: pass-vault.yaml

# ansible.cfg
remote_user: jenkins
use_copy: false
use_synchronize: true

# apt-postgres && deploy-app
db_name: elecciones
db_user: elecusr
db_pass: "{{ postgres_password }}"
db_server: localhost
db_port: 5432
db_min_pool_size: 5
db_max_pool_size: 50
db_datasources: elecciones-ds

# apt-wildfly
memoria_min: 2g
memoria_max: 3g